<odoo>
    <record id="view_forms_cctv_list" model="ir.ui.view">
        <field name="name">forms.cctv.list</field>
        <field name="model">forms.cctv</field>
        <field name="arch" type="xml">
            <list>
                <field name="incident_datetime"/>
                <field name="location"/>
                <field name="camera_number"/>
                <field name="footage_provided"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_cctv_forms_form" model="ir.ui.view">
        <field name="name">forms.cctv.form</field>
        <field name="model">forms.cctv</field>
        <field name="arch" type="xml">
            <form string="CCTV Request Form">
                <header>
                    <button name="action_submit"
                        string="Submit"
                        type="object"
                        class="btn-primary"
                        invisible="not can_submit"/>

                    <button name="action_under_review"
                            string="Under Review"
                            type="object"
                            class="btn-primary"
                            invisible="not can_under_review"/>

                    <button name="action_complete"
                            string="Complete"
                            type="object"
                            class="btn-primary"
                            invisible="not can_complete"/>

                    <button name="action_reject"
                            string="Reject"
                            type="object"
                            class="btn-secondary"
                            invisible="not can_reject"/>

                    <button name="action_cancel"
                            string="Cancel"
                            type="object"
                            class="btn-secondary"
                            invisible="not can_cancel"/>

                    <button name="action_draft"
                            string="Reset to Draft"
                            type="object"
                            class="btn-secondary"
                            invisible="not can_draft"/>
                    <field name="state" widget="statusbar" clickable="1" statusbar_visible="draft,submitted,under_review,completed,rejected,cancelled"/>
                </header>
                <sheet>
                    <widget name="web_ribbon" invisible="not state in ('draft')" title="Draft" bg_color="text-bg-secondary"/>
                    <widget name="web_ribbon" invisible="not state in ('submitted')" title="Submitted" bg_color="text-bg-primary"/>
                    <widget name="web_ribbon" invisible="not state in ('under_review')" title="Under Review" bg_color="text-bg-warning"/>
                    <widget name="web_ribbon" invisible="not state in ('completed')" title="Completed" bg_color="text-bg-success"/>
                    <widget name="web_ribbon" invisible="not state in ('rejected')" title="Rejected" bg_color="text-bg-dark"/>
                    <widget name="web_ribbon" invisible="not state in ('cancelled')" title="Cancelled" bg_color="text-bg-danger"/>
                    <div class="alert alert-warning" invisible="reviewer_ids">
                        ⚠ Please assign at least one reviewer before moving to next phase.
                    </div>

                    <group>
                        <group>
                            <field name="incident_datetime" readonly="is_locked"/>
                            <field name="location" readonly="is_locked"/>
                            <field name="camera_number" readonly="is_locked"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Incident Details">
                            <group>
                                <field name="purpose" readonly="is_locked"/>
                                <field name="description" readonly="is_locked"/>
                            </group>
                        </page>

                        <page string="Reviews / Remarks">
                            <group>
                                <field name="reviewer_ids"
                                       nolabel="1">
                                    <list editable="bottom">
                                        <field name="review_for" readonly="parent.review_section_locked"/>
                                        <field name="reviewer_id" readonly="parent.review_section_locked"/>
                                        <field name="state" readonly="parent.review_section_locked"/>
                                        <field name="remarks" readonly="parent.review_section_locked"/>
                                    </list>
                                </field>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_cctv_forms" model="ir.actions.act_window">
        <field name="name">CCTV Request Forms</field>
        <field name="res_model">forms.cctv</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
