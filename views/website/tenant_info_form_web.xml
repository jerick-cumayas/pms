<odoo>
    <template id="website_tenant_info_form" name="Tenant Info Form">
        <t t-call="website.layout">
            <div class="container my-5">
                <div class="row justify-content-center">
                    <div class="col-lg-10 col-md-12">

                        <div class="card shadow-sm">
                            <div class="card-body p-4">

                                <h2 class="card-title text-center mb-2">
                                    Tenant Information
                                </h2>

                                <p class="text-muted text-center mb-4">
                                    Please complete all applicable sections below.
                                </p>

                                <form t-att-action="'/forms/tenant-info/submit'"
                                      method="post"
                                      class="o_website_form d-flex flex-column gap-4"
                                      data-model_name="form.unit_owner_info"
                                      data-success_page="/forms/thank-you">

                                    <input type="hidden"
                                           name="csrf_token"
                                           t-att-value="request.csrf_token()"/>

                                    <!-- Personal Info -->
                                    <section class="border rounded p-3">
                                        <h4 class="mb-2">Personal Information</h4>
                                        <hr class="mt-0"/>
                                        <t t-call="pms.person_base_form"/>
                                    </section>

                                    <!-- Spouse Info -->
                                    <section class="border rounded p-3">
                                        <h4 class="mb-2">
                                            Spouse Information
                                            <span class="text-muted fs-6">(if married)</span>
                                        </h4>
                                        <hr class="mt-0"/>
                                        <t t-call="pms.spouse_person_base_form"/>
                                    </section>

                                    <!-- Vehicle Info -->
                                    <section class="border rounded p-3">
                                        <h4 class="mb-2">Vehicle Information</h4>
                                        <hr class="mt-0"/>
                                        <t t-call="pms.vehicle_base_form"/>
                                    </section>

                                    <!-- Tenant Members -->
                                    <section class="border rounded p-3">
                                        <div class="d-flex justify-content-between align-items-center mb-2">
                                            <h4 class="mb-0">Tenant Member Information</h4>
                                            <button type="button"
                                                    id="add-member"
                                                    class="btn btn-outline-secondary btn-sm">
                                                + Add Member
                                            </button>
                                        </div>
                                        <hr class="mt-2"/>
                                        <t t-call="pms.website_multiple_person_base_form"/>
                                    </section>

                                    <!-- Submit -->
                                    <div class="d-grid mt-3">
                                        <button type="submit"
                                                class="btn btn-primary btn-lg">
                                            Submit Information
                                        </button>
                                    </div>

                                </form>

                            </div>
                        </div>

                    </div>
                </div>
            </div>

            <!-- JS unchanged -->
            <script>
                const container = document.getElementById('member-container');

                function updateBadges() {
                    Array.from(container.children).forEach((card, index) => {
                        const badge = card.querySelector('.badge');
                        if (badge) badge.textContent = 'Member ' + (index + 1);
                    });
                }

                document.getElementById('add-member').addEventListener('click', () => {
                    const newMember = container.children[0].cloneNode(true);
                    newMember.querySelectorAll('input, select, textarea').forEach(el => {
                        if (el.type === 'checkbox') el.checked = false;
                        else el.value = '';
                    });
                    container.appendChild(newMember);
                    updateBadges();
                });

                container.addEventListener('click', e => {
                    if (e.target.classList.contains('remove-member')) {
                        if (container.children.length > 1) {
                            e.target.closest('.member-entry').remove();
                            updateBadges();
                        } else {
                            alert('At least one person must be entered.');
                        }
                    }
                });
            </script>
        </t>
    </template>
</odoo>
